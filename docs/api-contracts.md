# API contracts
MBN consumes the AXIRO Mini `/api/v1` customer marketplace API.

Primary surfaces: customer auth/profile, products, marketplace options, transactions, payments, disputes, documents, wallet, payout, media, notifications and Escrow Box.

Totals and lifecycle decisions are calculated by the API. MBN only presents API-owned state and submits explicit customer actions.

# Form Dark Canonicalization v66.19

- Restored the approved dark-form direction globally.
- One final owner remains: `src/styles/form-controls.css`.
- All canonical inputs, selects, textareas, password fields, money fields, filters and modal forms use dark backgrounds with white text.
- Placeholder, readonly, disabled, invalid, autofill, options and native date affordances are covered.
- `check:form-colors` now protects the dark surface and white-text contract.
- No page-level form-color implementation was introduced.
